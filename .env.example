POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=database
POSTGRES_PORT=5432
POSTGRES_HOST=orcid-acad-mgr-postgres
POSTGRES_DATABASE_URL=postgres://user:password@orcid-acad-mgr-postgres:5432/database
LOCAL_POSTGRES_DATABASE_URL=postgres://user:password@localhost:32770/database

FRONTEND_URL=http://localhost:5173

# URL del API que consume el contenedor cliente (runtime, solo esquema://host[:puerto], sin path).
API_URL=http://server:8000

# Origenes CORS del server como literal de array TOML. Vacio si todos los clientes pasan por el proxy nginx.
CORS_ALLOW_ORIGINS=[]

JWT_SECRET=jwt-secret

RESEND_API_KEY=key

SEED_ADMIN_EMAIL=admin
SEED_ADMIN_PASSWORD=admin
SEED_ADMIN_PASSWORD_HASH=admin

OPENALEX_API_KEY=key

SMTP_HOST=name.domain.cl
SMTP_PORT=465
SMTP_USERNAME=youremail@domain.cl
SMTP_PASSWORD=your_password
